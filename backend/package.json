{
  "name": "backend",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "db:clear": "rm -f data/db.sqlite data/db.sqlite-wal data/db.sqlite-shm",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "import:pb": "bun run scripts/migrate-from-pb.ts",
    "backup": "bun run scripts/backup.ts",
    "compress-images": "bun run scripts/compress-images.ts",
    "admin:install": "cd admin-panel && bun install",
    "admin:build": "cd admin-panel && bun run build",
    "admin:rebuild": "cd admin-panel && bun install && bun run build"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.2",
    "@elysiajs/jwt": "^1.4.2",
    "@elysiajs/swagger": "^1.3.1",
    "date-fns": "^4.3.0",
    "drizzle-orm": "^0.45.2",
    "elysia": "^1.4.28",
    "howlongtobeat": "^1.7.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "bun-types": "latest",
    "drizzle-kit": "^0.31.10"
  },
  "module": "src/index.js"
}
